# Log-transform
abundance <- apply(txi$abundance, MARGIN = c(1, 2), FUN = function(x) log10(x + 1))
# Long-format abundance
txi_long <- as.data.frame(abundance) %>%
rownames_to_column(var = "Transcript.ID") %>%
pivot_longer(-Transcript.ID, names_to = "ptid", values_to = "Expression")
# Ensure matching types
metadata$ptid <- as.character(metadata$ptid)
txi_long$ptid <- as.character(txi_long$ptid)
# Merge metadata
txi_long <- left_join(txi_long, metadata[, annotation_column], by = "ptid")
txi_long <- txi_long[order(txi_long$mainpathogen) , ]
txi_long <- txi_long %>%
mutate(
prefix = sub("_.*", "", Transcript.ID),  # grab everything before '_' (or entire string if '_' not present)
species = case_when(
startsWith(prefix, "HGG68") ~ "E. coli",
startsWith(prefix, "KPHS")  ~ "Klebsiella pneumoniae",
startsWith(prefix, "WMS")   ~ "Enterococcus faecalis",
startsWith(prefix, "PMI")   ~ "Proteus mirabilis",
startsWith(prefix, "PA")    ~ "Pseudomonas aeruginosa",
TRUE ~ "Unknown"
)
)
return(txi_long)
# Generate heatmap
heatmap <- tidyheatmap(
df = txi_long,
rows = Transcript.ID,
columns = ptid,
values = Expression,
annotation_col = annotation_column[1:length(annotation_column)],
annotation_row =
show_rownames = FALSE,
bt_heatmap <- function(txi,
metadata,
annotation_column = c("ptid", "uti", "ecoli", "klebsiella", "proteus", "enterococcus", "otheruropathogen", "pyelo_sbj", "mainpathogen"),
patients) {
# Ensure patient IDs are characters
patients <- as.character(patients)
# Identify missing patients
missing_patients <- setdiff(patients, colnames(txi$abundance))
# Create a zero-filled matrix for the missing patients
if (length(missing_patients) > 0) {
zeros_mat <- matrix(0, nrow = nrow(txi$abundance), ncol = length(missing_patients))
colnames(zeros_mat) <- missing_patients
rownames(zeros_mat) <- rownames(txi$abundance)
# Bind to original matrix
txi$abundance <- cbind(txi$abundance, zeros_mat)
}
# Reorder to match 'patients' vector
txi$abundance <- txi$abundance[, patients]
# Log-transform
abundance <- apply(txi$abundance, MARGIN = c(1, 2), FUN = function(x) log10(x + 1))
# Long-format abundance
txi_long <- as.data.frame(abundance) %>%
rownames_to_column(var = "Transcript.ID") %>%
pivot_longer(-Transcript.ID, names_to = "ptid", values_to = "Expression")
# Ensure matching types
metadata$ptid <- as.character(metadata$ptid)
txi_long$ptid <- as.character(txi_long$ptid)
# Merge metadata
txi_long <- left_join(txi_long, metadata[, annotation_column], by = "ptid")
txi_long <- txi_long[order(txi_long$mainpathogen) , ]
txi_long <- txi_long %>%
mutate(
prefix = sub("_.*", "", Transcript.ID),  # grab everything before '_' (or entire string if '_' not present)
species = case_when(
startsWith(prefix, "HGG68") ~ "E. coli",
startsWith(prefix, "KPHS")  ~ "Klebsiella pneumoniae",
startsWith(prefix, "WMS")   ~ "Enterococcus faecalis",
startsWith(prefix, "PMI")   ~ "Proteus mirabilis",
startsWith(prefix, "PA")    ~ "Pseudomonas aeruginosa",
TRUE ~ "Unknown"
)
)
return(txi_long)
# Generate heatmap
heatmap <- tidyheatmap(
df = txi_long,
rows = Transcript.ID,
columns = ptid,
values = Expression,
annotation_col = annotation_column[1:length(annotation_column)],
annotation_row = species,
show_rownames = FALSE,
colors = c("white", "red"),
cluster_rows = FALSE,
cluster_cols = FALSE
# breaks = seq(0, max(txi_long$Expression), length.out = 100)
)
return(list(heatmap = heatmap, txi_long = txi_long, abundance = abundance))
}
all_heatmap <- bt_heatmap(all_txi, reads.meta.counts.239, patients = ordered_k_groups_name)
all_heatmap$species
table(all_heatmap$species)
bt_heatmap <- function(txi,
metadata,
annotation_column = c("ptid", "uti", "ecoli", "klebsiella", "proteus", "enterococcus", "otheruropathogen", "pyelo_sbj", "mainpathogen"),
patients) {
# Ensure patient IDs are characters
patients <- as.character(patients)
# Identify missing patients
missing_patients <- setdiff(patients, colnames(txi$abundance))
# Create a zero-filled matrix for the missing patients
if (length(missing_patients) > 0) {
zeros_mat <- matrix(0, nrow = nrow(txi$abundance), ncol = length(missing_patients))
colnames(zeros_mat) <- missing_patients
rownames(zeros_mat) <- rownames(txi$abundance)
# Bind to original matrix
txi$abundance <- cbind(txi$abundance, zeros_mat)
}
# Reorder to match 'patients' vector
txi$abundance <- txi$abundance[, patients]
# Log-transform
abundance <- apply(txi$abundance, MARGIN = c(1, 2), FUN = function(x) log10(x + 1))
# Long-format abundance
txi_long <- as.data.frame(abundance) %>%
rownames_to_column(var = "Transcript.ID") %>%
pivot_longer(-Transcript.ID, names_to = "ptid", values_to = "Expression")
# Ensure matching types
metadata$ptid <- as.character(metadata$ptid)
txi_long$ptid <- as.character(txi_long$ptid)
# Merge metadata
txi_long <- left_join(txi_long, metadata[, annotation_column], by = "ptid")
txi_long <- txi_long[order(txi_long$mainpathogen) , ]
txi_long <- txi_long %>%
mutate(
prefix = sub("_.*", "", Transcript.ID),  # grab everything before '_' (or entire string if '_' not present)
species = case_when(
startsWith(prefix, "HGG68") ~ "E. coli",
startsWith(prefix, "KPHS")  ~ "Klebsiella pneumoniae",
startsWith(prefix, "WMS")   ~ "Enterococcus faecalis",
startsWith(prefix, "PMI")   ~ "Proteus mirabilis",
startsWith(prefix, "PA")    ~ "Pseudomonas aeruginosa",
TRUE ~ "Unknown"
)
)
# Generate heatmap
heatmap <- tidyheatmap(
df = txi_long,
rows = Transcript.ID,
columns = ptid,
values = Expression,
annotation_col = annotation_column[1:length(annotation_column)],
annotation_row = species,
show_rownames = FALSE,
colors = c("white", "red"),
cluster_rows = FALSE,
cluster_cols = FALSE
# breaks = seq(0, max(txi_long$Expression), length.out = 100)
)
return(list(heatmap = heatmap, txi_long = txi_long, abundance = abundance))
}
all_heatmap <- bt_heatmap(all_txi, reads.meta.counts.239, patients = ordered_k_groups_name)
bt_heatmap <- function(txi,
metadata,
annotation_column = c("ptid", "uti", "ecoli", "klebsiella", "proteus", "enterococcus", "otheruropathogen", "pyelo_sbj", "mainpathogen"),
patients) {
# Ensure patient IDs are characters
patients <- as.character(patients)
# Identify missing patients
missing_patients <- setdiff(patients, colnames(txi$abundance))
# Create a zero-filled matrix for the missing patients
if (length(missing_patients) > 0) {
zeros_mat <- matrix(0, nrow = nrow(txi$abundance), ncol = length(missing_patients))
colnames(zeros_mat) <- missing_patients
rownames(zeros_mat) <- rownames(txi$abundance)
# Bind to original matrix
txi$abundance <- cbind(txi$abundance, zeros_mat)
}
# Reorder to match 'patients' vector
txi$abundance <- txi$abundance[, patients]
# Log-transform
abundance <- apply(txi$abundance, MARGIN = c(1, 2), FUN = function(x) log10(x + 1))
# Long-format abundance
txi_long <- as.data.frame(abundance) %>%
rownames_to_column(var = "Transcript.ID") %>%
pivot_longer(-Transcript.ID, names_to = "ptid", values_to = "Expression")
# Ensure matching types
metadata$ptid <- as.character(metadata$ptid)
txi_long$ptid <- as.character(txi_long$ptid)
# Merge metadata
txi_long <- left_join(txi_long, metadata[, annotation_column], by = "ptid")
txi_long <- txi_long[order(txi_long$mainpathogen) , ]
txi_long <- txi_long %>%
mutate(
prefix = sub("_.*", "", Transcript.ID),  # grab everything before '_' (or entire string if '_' not present)
transcriptome = case_when(
startsWith(prefix, "HGG68") ~ "E. coli",
startsWith(prefix, "KPHS")  ~ "Klebsiella pneumoniae",
startsWith(prefix, "WMS")   ~ "Enterococcus faecalis",
startsWith(prefix, "PMI")   ~ "Proteus mirabilis",
startsWith(prefix, "PA")    ~ "Pseudomonas aeruginosa",
TRUE ~ "Unknown"
)
)
# Generate heatmap
heatmap <- tidyheatmap(
df = txi_long,
rows = Transcript.ID,
columns = ptid,
values = Expression,
annotation_col = annotation_column[1:length(annotation_column)],
annotation_row = transcriptome,
show_rownames = FALSE,
colors = c("white", "red"),
cluster_rows = FALSE,
cluster_cols = FALSE
# breaks = seq(0, max(txi_long$Expression), length.out = 100)
)
return(list(heatmap = heatmap, txi_long = txi_long, abundance = abundance))
}
# Testing
all_heatmap <- bt_heatmap(all_txi, reads.meta.counts.239, patients = ordered_k_groups_name)
# View(all_heatmap)
venn.df
View(pca_df)
View(pred.df)
pred.df[match(reads.meta.counts.239$ptid, rownames(pred.df)) , ]$group
# Add venn info
reads.meta.counts.239$venn_group <- pred.df[match(reads.meta.counts.239$ptid, rownames(pred.df)) , ]$group
bt_heatmap <- function(txi,
metadata,
annotation_column = c("ptid", "venn_group", "uti", "ecoli", "klebsiella", "proteus", "enterococcus", "otheruropathogen", "pyelo_sbj", "mainpathogen"),
patients) {
# Ensure patient IDs are characters
patients <- as.character(patients)
# Identify missing patients
missing_patients <- setdiff(patients, colnames(txi$abundance))
# Create a zero-filled matrix for the missing patients
if (length(missing_patients) > 0) {
zeros_mat <- matrix(0, nrow = nrow(txi$abundance), ncol = length(missing_patients))
colnames(zeros_mat) <- missing_patients
rownames(zeros_mat) <- rownames(txi$abundance)
# Bind to original matrix
txi$abundance <- cbind(txi$abundance, zeros_mat)
}
# Reorder to match 'patients' vector
txi$abundance <- txi$abundance[, patients]
# Log-transform
abundance <- apply(txi$abundance, MARGIN = c(1, 2), FUN = function(x) log10(x + 1))
# Long-format abundance
txi_long <- as.data.frame(abundance) %>%
rownames_to_column(var = "Transcript.ID") %>%
pivot_longer(-Transcript.ID, names_to = "ptid", values_to = "Expression")
# Ensure matching types
metadata$ptid <- as.character(metadata$ptid)
txi_long$ptid <- as.character(txi_long$ptid)
# Merge metadata
txi_long <- left_join(txi_long, metadata[, annotation_column], by = "ptid")
txi_long <- txi_long[order(txi_long$mainpathogen) , ]
txi_long <- txi_long %>%
mutate(
prefix = sub("_.*", "", Transcript.ID),  # grab everything before '_' (or entire string if '_' not present)
transcriptome = case_when(
startsWith(prefix, "HGG68") ~ "E. coli",
startsWith(prefix, "KPHS")  ~ "Klebsiella pneumoniae",
startsWith(prefix, "WMS")   ~ "Enterococcus faecalis",
startsWith(prefix, "PMI")   ~ "Proteus mirabilis",
startsWith(prefix, "PA")    ~ "Pseudomonas aeruginosa",
TRUE ~ "Unknown"
)
)
# Generate heatmap
heatmap <- tidyheatmap(
df = txi_long,
rows = Transcript.ID,
columns = ptid,
values = Expression,
annotation_col = annotation_column[1:length(annotation_column)],
annotation_row = transcriptome,
show_rownames = FALSE,
colors = c("white", "red"),
cluster_rows = FALSE,
cluster_cols = FALSE
# breaks = seq(0, max(txi_long$Expression), length.out = 100)
)
return(list(heatmap = heatmap, txi_long = txi_long, abundance = abundance))
}
# Testing
all_heatmap <- bt_heatmap(all_txi, reads.meta.counts.239, patients = colnames(pred.df))
bt_heatmap <- function(txi,
metadata,
annotation_column = c("ptid", "venn_group", "uti", "ecoli", "klebsiella", "proteus", "enterococcus", "otheruropathogen", "pyelo_sbj", "mainpathogen"),
patients) {
# Ensure patient IDs are characters
patients <- as.character(patients)
# Identify missing patients
missing_patients <- setdiff(patients, colnames(txi$abundance))
# Create a zero-filled matrix for the missing patients
if (length(missing_patients) > 0) {
zeros_mat <- matrix(0, nrow = nrow(txi$abundance), ncol = length(missing_patients))
colnames(zeros_mat) <- missing_patients
rownames(zeros_mat) <- rownames(txi$abundance)
# Bind to original matrix
txi$abundance <- cbind(txi$abundance, zeros_mat)
}
# Reorder to match 'patients' vector
txi$abundance <- txi$abundance[, patients]
# Log-transform
abundance <- apply(txi$abundance, MARGIN = c(1, 2), FUN = function(x) log10(x + 1))
# Long-format abundance
txi_long <- as.data.frame(abundance) %>%
rownames_to_column(var = "Transcript.ID") %>%
pivot_longer(-Transcript.ID, names_to = "ptid", values_to = "Expression")
# Ensure matching types
metadata$ptid <- as.character(metadata$ptid)
txi_long$ptid <- as.character(txi_long$ptid)
# Merge metadata
txi_long <- left_join(txi_long, metadata[, annotation_column], by = "ptid")
txi_long <- txi_long[order(txi_long$mainpathogen) , ]
txi_long <- txi_long %>%
mutate(
prefix = sub("_.*", "", Transcript.ID),  # grab everything before '_' (or entire string if '_' not present)
transcriptome = case_when(
startsWith(prefix, "HGG68") ~ "E. coli",
startsWith(prefix, "KPHS")  ~ "Klebsiella pneumoniae",
startsWith(prefix, "WMS")   ~ "Enterococcus faecalis",
startsWith(prefix, "PMI")   ~ "Proteus mirabilis",
startsWith(prefix, "PA")    ~ "Pseudomonas aeruginosa",
TRUE ~ "Unknown"
)
)
# Generate heatmap
heatmap <- tidyheatmap(
df = txi_long,
rows = Transcript.ID,
columns = ptid,
values = Expression,
annotation_col = annotation_column[1:length(annotation_column)],
annotation_row = transcriptome,
show_rownames = FALSE,
colors = c("white", "red"),
cluster_rows = FALSE,
cluster_cols = FALSE,
breaks = seq(0, max(txi_long$Expression), length.out = 100)
)
return(list(heatmap = heatmap, txi_long = txi_long, abundance = abundance))
}
all_heatmap <- bt_heatmap(all_txi, reads.meta.counts.239, patients = colnames(pred.df))
colnames(pred.df)
rownames(pred.df)
all_heatmap <- bt_heatmap(all_txi, reads.meta.counts.239, patients = rownames(pred.df))
bt_heatmap <- function(txi,
metadata,
annotation_column = c("ptid", "venn_group", "uti", "ecoli", "klebsiella", "proteus", "enterococcus", "otheruropathogen", "pyelo_sbj", "mainpathogen"),
patients) {
# Ensure patient IDs are characters
patients <- as.character(patients)
# Identify missing patients
missing_patients <- setdiff(patients, colnames(txi$abundance))
# Create a zero-filled matrix for the missing patients
if (length(missing_patients) > 0) {
zeros_mat <- matrix(0, nrow = nrow(txi$abundance), ncol = length(missing_patients))
colnames(zeros_mat) <- missing_patients
rownames(zeros_mat) <- rownames(txi$abundance)
# Bind to original matrix
txi$abundance <- cbind(txi$abundance, zeros_mat)
}
# Reorder to match 'patients' vector
txi$abundance <- txi$abundance[, patients]
# Log-transform
abundance <- apply(txi$abundance, MARGIN = c(1, 2), FUN = function(x) log10(x + 1))
# Long-format abundance
txi_long <- as.data.frame(abundance) %>%
rownames_to_column(var = "Transcript.ID") %>%
pivot_longer(-Transcript.ID, names_to = "ptid", values_to = "Expression")
# Ensure matching types
metadata$ptid <- as.character(metadata$ptid)
txi_long$ptid <- as.character(txi_long$ptid)
# Merge metadata
txi_long <- left_join(txi_long, metadata[, annotation_column], by = "ptid")
txi_long <- txi_long[order(txi_long$mainpathogen) , ]
txi_long <- txi_long %>%
mutate(
prefix = sub("_.*", "", Transcript.ID),  # grab everything before '_' (or entire string if '_' not present)
transcriptome = case_when(
startsWith(prefix, "HGG68") ~ "E. coli",
startsWith(prefix, "KPHS")  ~ "Klebsiella pneumoniae",
startsWith(prefix, "WMS")   ~ "Enterococcus faecalis",
startsWith(prefix, "PMI")   ~ "Proteus mirabilis",
startsWith(prefix, "PA")    ~ "Pseudomonas aeruginosa",
TRUE ~ "Unknown"
)
)
# Generate heatmap
heatmap <- tidyheatmap(
df = txi_long,
rows = Transcript.ID,
columns = ptid,
values = Expression,
annotation_col = annotation_column[1:length(annotation_column)],
annotation_row = transcriptome,
show_rownames = FALSE,
colors = c("white", "red"),
cluster_rows = FALSE,
cluster_cols = FALSE
)
return(list(heatmap = heatmap, txi_long = txi_long, abundance = abundance))
}
all_heatmap <- bt_heatmap(all_txi, reads.meta.counts.239, patients = rownames(pred.df))
bt_heatmap <- function(txi,
metadata,
annotation_column = c("ptid", "venn_group", "uti", "ecoli", "klebsiella", "proteus", "enterococcus", "otheruropathogen", "pyelo_sbj", "mainpathogen"),
patients) {
# Ensure patient IDs are characters
patients <- as.character(patients)
# Identify missing patients
missing_patients <- setdiff(patients, colnames(txi$abundance))
# Create a zero-filled matrix for the missing patients
if (length(missing_patients) > 0) {
zeros_mat <- matrix(0, nrow = nrow(txi$abundance), ncol = length(missing_patients))
colnames(zeros_mat) <- missing_patients
rownames(zeros_mat) <- rownames(txi$abundance)
# Bind to original matrix
txi$abundance <- cbind(txi$abundance, zeros_mat)
}
# Reorder to match 'patients' vector
txi$abundance <- txi$abundance[, patients]
# Log-transform
abundance <- apply(txi$abundance, MARGIN = c(1, 2), FUN = function(x) log10(x + 1))
# Long-format abundance
txi_long <- as.data.frame(abundance) %>%
rownames_to_column(var = "Transcript.ID") %>%
pivot_longer(-Transcript.ID, names_to = "ptid", values_to = "Expression")
# Ensure matching types
metadata$ptid <- as.character(metadata$ptid)
txi_long$ptid <- as.character(txi_long$ptid)
# Merge metadata
txi_long <- left_join(txi_long, metadata[, annotation_column], by = "ptid")
txi_long <- txi_long[order(txi_long$mainpathogen) , ]
txi_long <- txi_long %>%
mutate(
prefix = sub("_.*", "", Transcript.ID),  # grab everything before '_' (or entire string if '_' not present)
transcriptome = case_when(
startsWith(prefix, "HGG68") ~ "E. coli",
startsWith(prefix, "KPHS")  ~ "Klebsiella pneumoniae",
startsWith(prefix, "WMS")   ~ "Enterococcus faecalis",
startsWith(prefix, "PMI")   ~ "Proteus mirabilis",
startsWith(prefix, "PA")    ~ "Pseudomonas aeruginosa",
TRUE ~ "Unknown"
)
)
# assign mainpathogen colours
mainpathogen_colors <- c(
"E. coli" = "#E41A1C",            # Red
"E. coli / Proteus" = "#377EB8",  # Blue
"Enterococcus" = "#4DAF4A",       # Green
"Klebsiella" = "#984EA3",         # Purple
"Proteus" = "#FF7F00",            # Orange
"Pseudomonas Aeruginosa" = "#A65628"  # Brown
)
# assign venn colours
venn_group_colors <- c(
"All_Negative" = "#1B9E77",
"All_Positive" = "#D95F02",
"Clin_HR_Negative" = "#7570B3",
"MT_HR_Clin_HR_Negative" = "#E7298A",
"MT_HR_Clin_Tax_Negative" = "#66A61E",
"MT_HR_Negative" = "#E6AB02",
"MT_Tax_Negative" = "#A6761D",
"MT_Tax_Clin_Tax_Negative" = "#666666",
"Only_Clin_HR_Positive" = "#1F78B4",
"Only_Clin_Tax_Positive" = "#33A02C",
"Only_MT_HR_Positive" = "#FB9A99",
"Only_MT_Tax_Positive" = "#B15928"
)
# Generate heatmap
ann_colors <- list(
mainpathogen = mainpathogen_colors,
venn_group = venn_group_colors
)
heatmap <- tidyheatmap(
df = txi_long,
rows = Transcript.ID,
columns = ptid,
values = Expression,
annotation_col = annotation_column[1:length(annotation_column)],
annotation_row = transcriptome,
annotation_color = ann_colors,
show_rownames = FALSE,
colors = c("white", "red"),
cluster_rows = FALSE,
cluster_cols = FALSE
)
return(list(heatmap = heatmap, txi_long = txi_long, abundance = abundance))
}
all_heatmap <- bt_heatmap(all_txi, reads.meta.counts.239, patients = rownames(pred.df))
length(rownames(pred.df))
rpmdata.239
knitr::opts_chunk$set(echo = TRUE, warning = FALSE, message = FALSE)
rpmdata.239
rpmdata.239
pyelo_positive <- reads.meta.counts.239[ , reads.meta.counts.239$pyelo_sbj == 1]
pyelo_negative <- reads.meta.counts.239[ , reads.meta.counts.239$pyelo_sbj == 0]
length(pyelo_positive) + length(pyelo_negative)
pyelo_positive <- reads.meta.counts[ , reads.meta.counts$pyelo_sbj == 1]$ptid
pyelo_negative <- reads.meta.counts[ , reads.meta.counts$pyelo_sbj == 0]$ptid
length(pyelo_positive) + length(pyelo_negative)
View(reads.meta.counts[ , reads.meta.counts$pyelo_sbj == 1])
pyelo_positive <- reads.meta.counts.239[ , reads.meta.counts.239$pyelo_sbj == 1]$ptid
pyelo_negative <- reads.meta.counts.239[ , reads.meta.counts.239$pyelo_sbj == 0]$ptid
length(pyelo_positive) + length(pyelo_negative)
reads.meta.counts.239
View(reads.meta.counts.239)
reads.meta.counts.239$pyelo_sbj
pyelo_positive <- reads.meta.counts[reads.meta.counts$pyelo_sbj == 1 , ]$ptid
pyelo_negative <- reads.meta.counts[reads.meta.counts$pyelo_sbj == 0 , ]$ptid
length(pyelo_positive) + length(pyelo_negative)
View(pred.df)
save.image(file = "~/Downloads/jul4.RData")
